'use strict';
const assert=require('node:assert/strict'),fs=require('node:fs'),vm=require('node:vm'),path=require('node:path');
const root=path.resolve(__dirname,'..'),source=p=>fs.readFileSync(path.join(root,p),'utf8');
const clone=x=>JSON.parse(JSON.stringify(x));
const data=new Map(),key='clubs/test/memberships/card',wallet='clubs/test/economy/wallet';
let failCommit=false,uid='reader',localSaves=0;
const ref=p=>({path:p,collection:n=>ref(p+'/'+n),doc:n=>ref(p+'/'+n)});
const db={collection:n=>ref(n),runTransaction:async fn=>{const writes=[];const result=await fn({get:async r=>({exists:data.has(r.path),data:()=>clone(data.get(r.path))}),set:(r,v)=>writes.push([r.path,clone(v)]),update:(r,v)=>writes.push([r.path,{...data.get(r.path),...clone(v)}])});if(failCommit)throw Error('simulated network failure');for(const [p,v] of writes)data.set(p,v);return result;}};
const ctx=vm.createContext({window:{db},console,firebase:{auth:()=>({currentUser:{uid,isAnonymous:true}})},saveStudentLocal:()=>localSaves++,todayStr:()=> 'today'});
vm.runInContext(source('pilot-reading-save-2026-09-10.js'),ctx);
data.set(key,{userId:'card',createdByTeacher:true,claimedByUid:'reader',status:'active',cachedStats:{}});data.set(wallet,{balance:0,lifetimeEarned:0});
(async()=>{
const input={id:'qa_completion_1',userId:'card',clubId:'test',student:{id:'card'},entry:{type:'app',minutes:2,points:3},delta:{isApp:true}};
await ctx.window.BookiReadingSave.commit(input);assert.equal(data.get(key).cachedStats.totalMinutes,2);assert.equal(data.get(wallet).balance,3);assert(data.has(key+'/sessions/qa_completion_1'));
assert.equal((await ctx.window.BookiReadingSave.commit(input)).alreadySaved,true);assert.equal(data.get(wallet).balance,3);
failCommit=true;await assert.rejects(ctx.window.BookiReadingSave.commit({...input,id:'qa_completion_2'}));assert.equal(data.get(key).cachedStats.totalMinutes,2);assert(!data.has(key+'/sessions/qa_completion_2'));failCommit=false;
uid='other';await assert.rejects(ctx.window.BookiReadingSave.commit({...input,id:'qa_completion_3'}),e=>e.code==='reading/card-not-owned');uid='reader';
await assert.rejects(ctx.window.BookiReadingSave.commit({...input,id:'qa_completion_4',entry:{type:'app',minutes:9999,points:3}}));
const r=vm.createContext({window:{}});vm.runInContext(source('roster-store.js'),r);const roster=r.window.BookiRosterStore;
const slots=Array.from({length:30},(_,i)=>({userId:'slot'+i,name:'כרטיס פנוי '+String(i+1).padStart(2,'0'),createdByTeacher:true,cachedStats:{}}));
const planned=roster.plan({rosterCapacity:30},slots,['א','ב','ג']);assert.equal(planned.rename.length,3);assert.equal(planned.create.length,0);assert.equal(planned.archive.length,0);
const named=[{userId:'a',name:'א'},{userId:'b',name:'ב'},{userId:'c',name:'ג'}];const repaired=roster.plan({},[...slots,...named],[]);assert.equal(repaired.archive.length,3);assert(repaired.archive.every(roster.unused));assert.equal(repaired.limit,30);
assert.throws(()=>roster.plan({rosterCapacity:30},slots.map(s=>({...s,claimedByUid:'reader'})),['חדש']));
const script=source('script.js'),start=script.indexOf('function _todayReadersHtml'),end=script.indexOf('async function _renderNewClubView',start);
const names=vm.createContext({fbLoadUserProfile:async()=>({name:'ילד בדיקה'})});vm.runInContext(script.slice(start,end),names);
const rows=await names._resolveTodayReaderNames([{name:'כרטיס פנוי 01',claimedByUid:'reader',cachedStats:{lastReadAt:new Date().toISOString()}},{name:'אתמול',cachedStats:{lastReadAt:'2020-01-01'}}]);
const html=names._todayReadersHtml(rows);assert(html.includes('ילד בדיקה'));assert(!html.includes('כרטיס פנוי'));assert(!html.includes('אתמול'));
console.log('PASS: atomic reading + wallet; duplicate retry; network failure; wrong owner; invalid duration; roster capacity + legacy repair; claimed-card protection; today reader profile name.');
})().catch(e=>{console.error(e);process.exitCode=1});
